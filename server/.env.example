# server/.env.example (TEMPLATE - SAFE TO PUSH TO GITHUB)

# This is the connection string for your MongoDB database.
# Replace the placeholder with your own connection string.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster_url>/<database_name>

# This is a secret key for creating secure user tokens (JWT).
# Generate your own unique, long, and random string for this value.
JWT_SECRET=generate_your_own_secret_key_here

# This is the port your backend server will run on. 5000 is a good default.
PORT=5000